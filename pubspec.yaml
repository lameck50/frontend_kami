name: kami_geoloc
description: KamiGeoloc scaffold - Flutter app
publish_to: 'none'
version: 0.1.0
environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.0
  geolocator: ^9.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  socket_io_client: ^2.0.0
  jwt_decode: ^0.3.0
  intl: ^0.18.1
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  connectivity_plus: ^5.0.2
  pdf: ^3.10.4
  csv: ^5.0.2
  open_file: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
